# Neon Postgres connection string (Vercel injects this automatically once the
# Neon integration is attached to the project).
DATABASE_URL="postgresql://user:password@host.neon.tech/bridgetongue?sslmode=require"

# Public site URL, used for canonical links and OG tags.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Model provider for the explanation layer. Not required for the landing-page
# proof or the deterministic grader; only the "why was I wrong" copy uses it.
ANTHROPIC_API_KEY=""
